Use factory method to create PolicyHolder and Dependent objects

regex pattern for claimid [f]*\d{10}
regex for customerid [f]*\d{7}
regex for cardnumber \d{10}
regex for document: [f]*\d{10}+_+\d{10}+_+[f]*\d{7}
old write
/*
        File file = new File(fileName+".txt");
        try(
        FileOutputStream fileOutput = new FileOutputStream(fileName,true);
        ObjectOutputStream objectOutput = new ObjectOutputStream(fileOutput);){
        objectOutput.writeObject(object);}
        catch (IOException e){
            System.err.printf("Error write to file\n");
        }*/
Old read file
/*
        File myFile = new File(fileName + ".txt");
        try (BufferedReader reader = new BufferedReader(new FileReader(myFile))){
            String nextLine;
            while ((nextLine = reader.readLine()) != null)
            {

            }
        }catch (IOException e){
            System.err.printf("% file not found\n",myFile);
        }*/